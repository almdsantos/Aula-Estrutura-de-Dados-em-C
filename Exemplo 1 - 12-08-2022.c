#include <stdio.h>
#include <stdlib.h>

main()
{

    int a, b;
    float media;
    printf("Entre com um inteiro a:");
    scanf("%d",&a);
    printf("Entre com um inteiro b:");
    scanf("%d",&b);
    media = (a + b)/2.0;
    printf("A média da operação é %f",media);
}
